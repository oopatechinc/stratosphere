version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.3
  pre_build:
    commands:
      - echo Installing composer packages...
      - composer install --ignore-platform-reqs
  build:
    commands:
      - echo run tests...
#      - php artisan test
reports:
  jest_reports:
    files:
      - junit.xml
    file-format: JUNITXML
